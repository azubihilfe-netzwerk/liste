import React, { FC } from 'react';

const Impressum: FC = () => {
    return (
        <div className="min-h-screen bg-blue-100 flex items-center justify-center text-red-600">
            <h1 className="text-4xl font-bold text-green-600">
                Impressum
            </h1>
        </div>
    );
};

export default Impressum;
