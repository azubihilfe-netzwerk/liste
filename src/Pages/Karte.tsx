import React, { FC } from 'react';

const Karte: FC = () => {
    return (
        <div className="min-h-screen flex flex-col items-center justify-start pt-6 ">
            <h1 className="text-4xl font-bold text-navbar-blue">Karte</h1>
        </div>
    );
};

export default Karte;